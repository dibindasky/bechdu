import 'package:flutter/material.dart';

class ScreenPickUp extends StatelessWidget {
  const ScreenPickUp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ScreenPickUp'),
      ),
    );
  }
}
